---
phase: 05-small-enhancements-and-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/metadata.js, tests/metadata.test.js, cli.js]
autonomous: true
requirements: []

must_haves:
  truths:
    - "Markdown files include human-readable ISO dates alongside Unix timestamps"
    - "Markdown files include tags from Pocket CSV in YAML array format"
    - "Markdown files preserve defuddle frontmatter metadata (title, author, published_date, site_name)"
    - "Metadata merge strategy combines Pocket context (tags, timestamps) with defuddle article data"
    - "Missing defuddle metadata fields degrade gracefully without crashes"
  artifacts:
    - path: "src/metadata.js"
      provides: "Enhanced metadata creation with tags, ISO dates, and defuddle merge"
      exports: ["createMetadata", "formatFrontmatter", "parseDefuddleFrontmatter"]
    - path: "tests/metadata.test.js"
      provides: "Comprehensive tests for new metadata fields and edge cases"
      min_lines: 100
    - path: "cli.js"
      provides: "Integration of enhanced metadata with CSV tags and defuddle responses"
      contains: "parseDefuddleFrontmatter"
  key_links:
    - from: "cli.js"
      to: "src/metadata.js:parseDefuddleFrontmatter"
      via: "defuddle response parsing before content extraction"
      pattern: "parseDefuddleFrontmatter\\(apiResponse\\)"
    - from: "src/metadata.js:createMetadata"
      to: "tags parameter"
      via: "CSV tags field passed from cli.js"
      pattern: "tags:\\s*\\["
---

<objective>
Enhance metadata output with human-readable dates, Pocket tags, and defuddle frontmatter preservation.

Purpose: Provide richer, more useful metadata in generated markdown files by combining Pocket's user context (when saved, tags) with defuddle's article metadata (author, published date, site info).

Output: Enhanced markdown files with complete YAML frontmatter containing both Pocket and defuddle metadata fields.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-small-enhancements-and-bug-fixes/05-CONTEXT.md
@src/metadata.js
@cli.js
@.planning/phases/03-markdown-output/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add defuddle frontmatter parser to metadata module</name>
  <files>src/metadata.js, tests/metadata.test.js</files>
  <behavior>
    - parseDefuddleFrontmatter() extracts YAML frontmatter from defuddle API response
    - Returns {frontmatter: Object, content: String} where frontmatter contains parsed YAML fields
    - Handles malformed YAML gracefully: returns {frontmatter: null, content: full_response} and does not throw
    - Handles missing frontmatter: returns {frontmatter: null, content: response}
    - Handles empty response: returns {frontmatter: null, content: ''}
  </behavior>
  <action>
Create `parseDefuddleFrontmatter(apiResponse)` function in src/metadata.js:
- Check if response starts with `---\n` (YAML frontmatter delimiter)
- Find closing `\n---\n` delimiter (skip first 4 chars to avoid matching opening delimiter)
- Extract frontmatter block between delimiters
- Parse YAML using js-yaml library (already in package.json from Phase 3)
- Wrap YAML parsing in try/catch to handle malformed YAML gracefully
- Return {frontmatter: parsed_object, content: content_after_frontmatter}
- If no frontmatter or parsing fails: return {frontmatter: null, content: original_response}

Use TDD approach:
1. Write tests first covering all behavior cases (malformed YAML, missing frontmatter, valid frontmatter, empty response)
2. Implement minimal code to pass tests
3. Refactor for clarity

Export parseDefuddleFrontmatter from module.exports.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=metadata.test.js --testNamePattern="parseDefuddleFrontmatter"</automated>
  </verify>
  <done>parseDefuddleFrontmatter function exists, handles all edge cases gracefully, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enhance createMetadata to support tags and ISO dates</name>
  <files>src/metadata.js, tests/metadata.test.js</files>
  <behavior>
    - createMetadata() accepts new optional `tags` parameter (array of strings)
    - Adds `tags` field to returned metadata object
    - Converts Unix timestamp to both `timestamp` (original) and `date` (ISO 8601) fields
    - Empty tags array becomes `tags: []` in YAML
    - Existing behavior preserved: title, url, date still work
  </behavior>
  <action>
Enhance createMetadata function signature:
- Add optional `tags` parameter: `function createMetadata({ title, url, timestamp, tags } = {})`
- Keep existing timestamp-to-ISO conversion logic
- Add new field to returned object: `timestamp: timestamp` (Unix timestamp from Pocket)
- Include `tags` in returned object (default to empty array if not provided)

Update formatFrontmatter to include new fields in YAML output:
- Add `timestamp` field after `date` field
- Add `tags` field as YAML array: `tags: [tag1, tag2, tag3]` or `tags: []`
- Use existing quoteIfNeeded logic for tag values

Enhance mergeDefuddleMetadata function (new):
- Accepts `defuddleFrontmatter` object and `pocketMetadata` object
- Merge strategy: defuddle fields (title, author, published_date, site_name) + pocket fields (tags, timestamp, date, url)
- Defuddle title takes precedence over Pocket title (more accurate)
- Return merged object

Use TDD: Write tests for tags array formatting, timestamp preservation, ISO date generation, merge strategy.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=metadata.test.js --testNamePattern="createMetadata|formatFrontmatter|merge"</automated>
  </verify>
  <done>createMetadata supports tags and preserves timestamp, formatFrontmatter outputs both fields, merge function combines Pocket and defuddle metadata correctly, tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Integrate enhanced metadata into CLI workflow</name>
  <files>cli.js</files>
  <action>
Update processArticles() function in cli.js:

1. Extract tags from CSV row:
   - After parsing CSV with PapaParse, extract `row.tags` field
   - Split on commas, trim whitespace: `const tags = row.tags ? row.tags.split(',').map(t => t.trim()).filter(t => t) : []`
   - Pass tags to file writer with other article data

2. Parse defuddle response before content extraction:
   - Import parseDefuddleFrontmatter from src/metadata.js
   - After successful fetchArticleContent(), call parseDefuddleFrontmatter(apiResponse)
   - Extract {frontmatter, content} from result
   - If frontmatter is null and content differs from apiResponse, log warning to stderr: `console.error('Warning: Malformed YAML in response from ${url}')`
   
3. Update processApiResponse() call:
   - Replace current processApiResponse(apiResponse) with direct use of `content` from parseDefuddleFrontmatter
   - Remove processApiResponse() function entirely (no longer needed - was just stripping frontmatter, now we parse it)

4. Pass complete metadata to writeArticleFile():
   - Change from `writeArticleFile(title, content, url, timestamp, outputDir)` 
   - To: `writeArticleFile(title, content, url, timestamp, outputDir, tags, defuddleFrontmatter)`
   - Update src/file-writer.js signature to accept new parameters
   - Inside file-writer, use mergeDefuddleMetadata to combine Pocket and defuddle fields before calling formatFrontmatter

Preserve existing behavior:
- Article processing loop unchanged
- Error handling unchanged  
- Progress reporting unchanged
- Rate limiting unchanged
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=integration.test.js</automated>
  </verify>
  <done>CLI extracts tags from CSV, parses defuddle frontmatter, merges metadata, passes to file writer, integration tests pass</done>
</task>

</tasks>

<verification>
End-to-end test:
1. Run CLI with test CSV containing articles with tags
2. Verify output markdown files contain:
   - `timestamp: "1234567890"` (Unix)
   - `date: "2024-03-15T10:30:00Z"` (ISO)
   - `tags: [programming, javascript, tutorial]`
   - Defuddle fields (title, author, published_date, site_name) if available
3. Verify malformed YAML in defuddle response logs warning but doesn't crash
4. Verify missing defuddle metadata fields don't crash processing
</verification>

<success_criteria>
- Markdown files include both Unix timestamp and ISO date fields
- Tags from Pocket CSV appear in YAML frontmatter as arrays
- Defuddle frontmatter metadata preserved and merged with Pocket metadata
- Malformed YAML in defuddle responses handled gracefully with warning
- Missing defuddle metadata fields don't cause crashes
- All existing functionality preserved (no regressions)
- Test coverage includes all new metadata fields and edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/05-small-enhancements-and-bug-fixes/05-01-SUMMARY.md`
</output>
