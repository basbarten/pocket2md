---
phase: 01-cli-and-csv-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [cli.js, package.json]
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03]
must_haves:
  truths:
    - "User can run `pocket2md --input data/part_000000.csv` and CLI starts"
    - "User can specify output directory with `--output ./output/` or use default"  
    - "User sees help when running with `--help` or without arguments"
  artifacts:
    - path: "cli.js"
      provides: "Main CLI entry point"
      min_lines: 50
      exports: []
    - path: "package.json"
      provides: "CLI binary configuration"
      contains: "bin"
  key_links:
    - from: "package.json"
      to: "cli.js"
      via: "bin field"
      pattern: '"bin".*"cli.js"'
---

<objective>
Establish the command-line interface foundation for the Pocket to Markdown CLI tool.

Purpose: Create the entry point and argument parsing that allows users to invoke the tool with proper flags and get expected help/error messages.
Output: Working CLI that accepts --input and --output flags and provides user feedback.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md  
@.planning/STATE.md
@.planning/phases/01-cli-and-csv-foundation/01-CONTEXT.md

<interfaces>
<!-- Key types and contracts from existing codebase -->
From package.json:
```json
{
  "name": "pocket2md",
  "version": "1.0.0", 
  "main": "cli.js"
}
```

From data/part_000000.csv (sample format):
```csv
title,url,time_added,tags,status
Compose to a Vertical Rhythm,http://24ways.org/2006/compose-to-a-vertical-rhythm/,1407757755,webdesign,archive
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Configure CLI binary in package.json</name>
  <files>package.json</files>
  <action>
    Add bin field to package.json to enable global installation as `pocket2md` command:
    - Add `"bin": { "pocket2md": "./cli.js" }` to package.json
    - Ensure cli.js will be the entry point when installed globally
    - Keep existing fields (name, version, description, main, scripts, author, license)
  </action>
  <verify>
    <automated>node -e "console.log(JSON.parse(require('fs').readFileSync('package.json')).bin)" | grep pocket2md</automated>
  </verify>
  <done>package.json contains bin field pointing pocket2md to cli.js</done>
</task>

<task type="auto">
  <name>task 2: Create CLI entry point with argument parsing</name>
  <files>cli.js</files>
  <action>
    Create cli.js as main CLI entry point with proper argument parsing:
    - Start with shebang `#!/usr/bin/env node` for Unix execution
    - Parse command-line arguments for --input/-i and --output/-o flags
    - Set default output directory to './output/'
    - Implement --help/-h flag showing usage and examples
    - Implement --version/-v flag showing version from package.json
    - Show help and exit when run without arguments
    - Validate required --input flag is provided
    - Print clear error for missing input file or invalid arguments
    - Use Node.js built-in process.argv parsing (no external dependencies yet)
    - Follow user decision: "Show help and exit when run without arguments"
    - Follow user decision: "Global install approach" so support both global and local execution
  </action>
  <verify>
    <automated>node cli.js --help | grep -E "(input|output)" && node cli.js 2>&1 | grep -E "(help|usage)"</automated>
  </verify>
  <done>CLI shows help, accepts --input and --output flags, validates required arguments</done>
</task>

<task type="auto">
  <name>task 3: Add basic file validation and startup feedback</name>
  <files>cli.js</files>
  <action>
    Enhance cli.js with input validation and user feedback:
    - Check if input file exists and is readable before proceeding
    - Show clear error message if input file not found: "Error: Input file not found: {filepath}"
    - Create output directory if it doesn't exist (using fs.mkdirSync with recursive: true)
    - Print startup message: "Processing Pocket export: {input_file}"
    - Print output location: "Output directory: {output_dir}"
    - Exit with code 1 for validation errors, code 0 for help/version
    - Follow user decision: "Early validation: check file access upfront, fail fast with clear messages"
    - Use Node.js built-in fs module for file operations
  </action>
  <verify>
    <automated>node cli.js --input nonexistent.csv 2>&1 | grep "not found" && node cli.js --input data/part_000000.csv --output test_output 2>&1 | grep "Processing"</automated>
  </verify>
  <done>CLI validates input file exists and creates output directory with clear user feedback</done>
</task>

</tasks>

<verification>
Run the complete CLI to verify core functionality:
- `node cli.js --help` shows usage information
- `node cli.js` shows help (no arguments provided)
- `node cli.js --input data/part_000000.csv` processes sample file
- `node cli.js --input missing.csv` shows clear error message
- CLI creates output directory if needed
</verification>

<success_criteria>
- User can run CLI with --input flag and see processing start
- User gets help when running without arguments or with --help
- User can specify output directory or use default ./output/
- Missing input files produce clear error messages
- CLI follows Unix conventions for flags and help
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-and-csv-foundation/01-01-SUMMARY.md`
</output>