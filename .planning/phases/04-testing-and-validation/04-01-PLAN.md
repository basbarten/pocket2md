---
phase: 04-testing-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["tests/fixtures/", "tests/integration-validation.test.js", "tests/manual-validation/"]
autonomous: true
requirements: ["CLI-01", "CLI-02", "CLI-03", "CSV-01", "CSV-02", "CSV-03", "FETCH-01", "FETCH-02", "FETCH-03", "API-01", "API-02", "API-03", "API-04", "MD-01", "MD-02", "MD-03", "MD-04"]

must_haves:
  truths:
    - "All 17 v1 requirements can be systematically validated"
    - "Comprehensive test data covers edge cases and error scenarios"
    - "Integration tests validate end-to-end workflow with mocked APIs"
    - "Manual validation process is documented and ready to execute"
  artifacts:
    - path: "tests/fixtures/"
      provides: "Comprehensive test CSV files for edge cases"
      min_files: 5
    - path: "tests/integration-validation.test.js"
      provides: "End-to-end integration tests with API mocking"
      exports: ["describe", "test cases for all requirements"]
    - path: "tests/manual-validation/"
      provides: "Manual validation checklist and documentation"
      contains: "requirement-by-requirement validation process"
  key_links:
    - from: "tests/integration-validation.test.js"
      to: "cli.js"
      via: "child_process.exec or direct function calls"
      pattern: "require.*cli|exec.*cli\\.js"
    - from: "tests/fixtures/"
      to: "tests/integration-validation.test.js"
      via: "test data import and usage"
      pattern: "fixtures.*csv"
    - from: "tests/manual-validation/"
      to: ".planning/REQUIREMENTS.md"
      via: "requirement mapping and validation checklist"
      pattern: "CLI-\\d+|CSV-\\d+|FETCH-\\d+|API-\\d+|MD-\\d+"
---

<objective>
Create comprehensive testing infrastructure for Phase 4 validation with test fixtures, integration tests, and manual validation framework.

Purpose: Establish the foundation for validating all 17 v1 requirements work correctly in the complete end-to-end workflow.
Output: Testing infrastructure ready for comprehensive validation execution.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-testing-and-validation/04-CONTEXT.md
@.planning/phases/04-testing-and-validation/04-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive test fixtures</name>
  <files>tests/fixtures/</files>
  <action>
Create `tests/fixtures/` directory with comprehensive test CSV files covering edge cases per CONTEXT decisions:

1. **tests/fixtures/empty.csv** - Empty file for testing empty input handling
2. **tests/fixtures/malformed.csv** - CSV with missing headers, invalid format
3. **tests/fixtures/special-chars.csv** - URLs and titles with special characters, unicode
4. **tests/fixtures/large-sample.csv** - 50+ articles for performance/memory validation
5. **tests/fixtures/mixed-domains.csv** - Various URL patterns and domain types
6. **tests/fixtures/network-errors.csv** - URLs that will trigger 404s, timeouts (use invalid domains)

Each CSV should follow Pocket export format with columns: url, title, excerpt, labels, time_added, time_read, time_favorited
Reference existing `data/part_000000.csv` for proper format structure.
Create README.md in fixtures explaining each test file's purpose and expected behavior.
  </action>
  <verify>
    <automated>ls tests/fixtures/*.csv | wc -l | grep -E "^[5-9]|[0-9]{2}"</automated>
  </verify>
  <done>tests/fixtures/ contains at least 5 test CSV files with documented edge cases, plus README.md explaining their purposes</done>
</task>

<task type="auto">
  <name>Task 2: Create integration validation test suite</name>
  <files>tests/integration-validation.test.js</files>
  <action>
Create comprehensive integration test file that validates all 17 v1 requirements using Jest and existing mock patterns:

1. **Setup mocking** - Mock defuddle.md API responses using Jest mocks, handle various response scenarios
2. **CLI interface tests** - Test all CLI-01, CLI-02, CLI-03 requirements with different argument combinations
3. **CSV processing tests** - Test CSV-01, CSV-02, CSV-03 with fixture files (empty, malformed, valid)
4. **Fetch/API tests** - Test FETCH-01/02/03 and API-01/02/03/04 with mocked responses (success, 404, timeout, rate limiting)
5. **Markdown output tests** - Test MD-01/02/03/04 by running complete workflow and validating output files

Use existing patterns from `tests/integration.test.js` for temp directory setup and cleanup.
Each test should map to specific requirements and validate expected behavior vs. actual behavior.
Include performance test with large-sample.csv to validate memory usage stays reasonable.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=integration-validation</automated>
  </verify>
  <done>Integration test suite covers all 17 v1 requirements with mocked external dependencies and validates end-to-end workflow</done>
</task>

<task type="auto">
  <name>Task 3: Create manual validation framework</name>
  <files>tests/manual-validation/</files>
  <action>
Create manual validation directory with systematic requirement validation process:

1. **checklist.md** - Requirement-by-requirement validation checklist with:
   - Each of 17 v1 requirements (CLI-01 through MD-04) 
   - Specific test action for each requirement
   - Expected result description
   - Pass/fail checkbox
   - Notes section for observations

2. **validation-process.md** - Step-by-step manual validation process:
   - Pre-validation setup (test data, network conditions)
   - Complete workflow execution steps
   - Error scenario testing procedures
   - Performance/memory observation guidelines
   - Final report generation instructions

3. **test-scenarios.md** - Detailed test scenarios for manual execution:
   - Happy path: Complete workflow with sample CSV
   - Error scenarios: Network failures, malformed data, file system issues
   - Edge cases: Large datasets, special characters, empty inputs
   - Performance scenarios: Memory usage, processing time validation

Reference CONTEXT.md decisions about "simple verification table format" and "pass/fail report" approach.
  </action>
  <verify>
    <automated>ls tests/manual-validation/*.md | wc -l | grep -E "^[3-9]"</automated>
  </verify>
  <done>Manual validation framework provides structured process for validating all 17 requirements with checklists, procedures, and test scenarios</done>
</task>

</tasks>

<verification>
Test infrastructure validation:
- Run `npm test` to ensure all existing tests still pass
- Verify new integration tests execute without errors
- Confirm test fixtures cover documented edge cases
- Validate manual validation framework is complete and actionable
</verification>

<success_criteria>
- Comprehensive test fixtures created covering all major edge cases identified in research
- Integration test suite provides automated validation for all 17 v1 requirements
- Manual validation framework enables systematic requirement-by-requirement verification
- All tests pass and infrastructure is ready for execution validation
- Testing approach aligns with user decisions from CONTEXT.md (manual validation primary, unit tests on critical paths, mocked APIs)
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-and-validation/04-01-SUMMARY.md`
</output>