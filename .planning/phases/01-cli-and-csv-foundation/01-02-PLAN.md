---
phase: 01-cli-and-csv-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [cli.js, package.json]
autonomous: true  
requirements: [CSV-01, CSV-02, CSV-03]
must_haves:
  truths:
    - "CLI reads Pocket export CSV format successfully"
    - "CLI extracts article URLs, titles, tags, and dates from CSV rows"
    - "CLI handles malformed CSV files with clear error messages"
    - "CSV parsing errors are logged but don't halt processing"
  artifacts:
    - path: "cli.js"
      provides: "CSV parsing implementation"
      min_lines: 100
      contains: "papaparse"
    - path: "package.json"  
      provides: "CSV parsing dependency"
      contains: "papaparse"
  key_links:
    - from: "cli.js"
      to: "papaparse"
      via: "require statement"
      pattern: "require.*papaparse"
    - from: "CLI argument parser"
      to: "CSV parsing logic"
      via: "input file processing"
      pattern: "readFileSync.*\\.csv"
---

<objective>
Implement CSV parsing capability to read Pocket export files and extract article data.

Purpose: Enable the CLI to parse real Pocket CSV exports, extract relevant fields (URL, title, tags, time_added), and handle parsing errors gracefully while providing user feedback.
Output: Working CSV processor that extracts article data and reports progress/errors.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-and-csv-foundation/01-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->
From cli.js (created in Plan 01):
```javascript
// CLI argument structure (will be available)
const args = {
  input: string,    // path to CSV file  
  output: string    // output directory path
};
```

Expected CSV format from data/part_000000.csv:
```csv
title,url,time_added,tags,status
"Article Title",http://example.com,1407757755,"tag1|tag2",archive
```

Article data structure to extract:
```javascript
const article = {
  title: string,
  url: string, 
  time_added: string,
  tags: string
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Install and configure PapaParse CSV library</name>
  <files>package.json</files>
  <action>
    Add PapaParse dependency to package.json for robust CSV parsing:
    - Run npm install to add papaparse library (CommonJS compatible)
    - PapaParse chosen per requirements and user discussion for reliable CSV parsing
    - Verify package.json dependencies section includes papaparse
    - Use exact version to ensure consistent behavior across installs
  </action>
  <verify>
    <automated>npm list papaparse 2>/dev/null | grep papaparse && grep -q papaparse package.json</automated>
  </verify>
  <done>PapaParse library installed and added to package.json dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Implement CSV parsing with progress reporting</name>
  <files>cli.js</files>
  <behavior>
    - Parse CSV file using PapaParse with header detection
    - Extract title, url, time_added, and tags fields from each row
    - Show progress as "Processing article N/M..." for each valid row
    - Skip rows missing required fields (especially URL) with warning
    - Collect articles into array for processing by next phases
  </behavior>
  <action>
    Add CSV parsing logic to cli.js after argument validation:
    - Import PapaParse library: `const Papa = require('papaparse')`
    - Read CSV file content using fs.readFileSync
    - Parse CSV with Papa.parse using header: true for column name detection
    - Validate expected columns exist: title, url, time_added, tags
    - Process each data row:
      * Extract title, url, time_added, tags fields
      * Skip rows where url is empty/missing (log warning with row number)
      * Show progress: "Processing article {current}/{total}..." per user decision format
      * Count successful vs skipped articles
    - Follow user decision: "Flexible by header: use header row to find columns by name regardless of order"
    - Follow user decision: "Skip malformed rows and log which line number had issues"
    - Collect valid articles in array for next phase processing
    - Show final summary: "Processed {total} articles: {successful} successful, {failed} failed"
  </action>
  <verify>
    <automated>node cli.js --input data/part_000000.csv --output test_output 2>&1 | grep -E "Processing article [0-9]+/[0-9]+" && test -d test_output</automated>
  </verify>
  <done>CLI parses CSV file, shows progress for each article, and reports summary statistics</done>
</task>

<task type="auto">
  <name>task 3: Add comprehensive error handling for CSV edge cases</name>
  <files>cli.js</files>
  <action>
    Enhance CSV parsing with robust error handling:
    - Wrap Papa.parse in try-catch to handle file encoding issues
    - Check for empty CSV files and show warning: "Warning: CSV file is empty"
    - Validate required columns exist in header: "Error: CSV missing required columns: {missing_columns}"
    - Handle malformed CSV structure (Papa.parse errors) gracefully
    - Show specific error for files that aren't valid CSV format
    - Follow user decision: "UTF-8 strict encoding requirement" - fail if encoding issues
    - Follow user decision: "Empty CSV handling: show warning but continue processing"
    - Follow user decision: "Simple exit codes: 0 for success, 1 for any failure"
    - Continue processing valid rows even when some rows are malformed
    - Log detailed error info but maintain user-friendly main messages
  </action>
  <verify>
    <automated>echo "invalid,csv" > /tmp/bad.csv && node cli.js --input /tmp/bad.csv 2>&1 | grep -i error; echo "" > /tmp/empty.csv && node cli.js --input /tmp/empty.csv 2>&1 | grep -i "empty"</automated>
  </verify>
  <done>CSV parser handles encoding errors, empty files, and malformed structure with clear error messages</done>
</task>

</tasks>

<verification>
Test CSV parsing with various inputs:
- `node cli.js --input data/part_000000.csv` processes real Pocket export successfully
- Shows progress indicators: "Processing article 1/N", "Processing article 2/N" etc.
- Displays final summary with success/failure counts
- Handles empty CSV files gracefully
- Reports clear errors for malformed CSV files
- Extracts all expected fields (title, url, time_added, tags) from valid rows
</verification>

<success_criteria>
- CLI successfully parses real Pocket CSV export format
- User sees progress reporting during processing: "Processing article N/M..."
- Malformed CSV files produce clear error messages without crashing
- Empty or invalid CSV files are handled gracefully
- All article fields (title, url, tags, time_added) are extracted correctly
- Final summary shows total articles processed and any errors encountered
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-and-csv-foundation/01-02-SUMMARY.md`
</output>