---
phase: 02-content-extraction
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [cli.js]
autonomous: true
requirements: [FETCH-01, FETCH-02, API-02, API-03]

must_haves:
  truths:
    - "CLI implements 10-second rate limiting between API calls"
    - "Network errors don't crash the application"
    - "HTTP errors (404, 500, timeouts) are handled gracefully"
    - "CLI continues processing after API failures"
    - "User sees clear error messages for failed requests"
  artifacts:
    - path: "cli.js"
      provides: "Rate limiting implementation"
      contains: "setTimeout.*10000"
    - path: "cli.js"
      provides: "Comprehensive error handling"
      contains: "try.*catch.*fetch"
  key_links:
    - from: "API call loop"
      to: "rate limiting delay"
      via: "setTimeout promise"
      pattern: "setTimeout.*resolve.*10.*seconds?"
    - from: "fetch requests"
      to: "error handling"
      via: "try-catch blocks"
      pattern: "try.*fetch.*catch.*error"
---

<objective>
Implement rate limiting and comprehensive error handling for API requests

Purpose: Ensure respectful API usage with 10-second delays and robust error handling that doesn't crash the CLI
Output: Production-ready API integration with proper rate limiting and error resilience
</objective>

<execution_context>
@$HOME/.config/Claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-extraction/02-01-SUMMARY.md

<interfaces>
<!-- Expected interfaces from 02-01 plan -->

From cli.js (expected after 02-01):
```javascript
// URL validation function
function validateUrl(url) {
  // Returns boolean for URL validity
}

// API integration function
async function fetchArticleContent(url) {
  // Fetches from defuddle.md API
  // Returns extracted markdown content
}

// Article processing loop structure
for (let i = 0; i < articles.length; i++) {
  const article = articles[i];
  // Processing logic with progress reporting
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: implement rate limiting with 10-second delays</name>
  <files>cli.js</files>
  <action>
    Add rate limiting to the API request loop to respect the defuddle.md service.
    
    Implementation:
    - Add 10-second delay between API requests (not before the first request)
    - Use setTimeout with Promise wrapper for async delays
    - Show countdown or waiting message to user during delays
    - Skip delay for the first article to start processing immediately
    - Update progress messages to show "Waiting 10 seconds before next request..."
    
    Pattern: After each successful or failed API request (except the last), wait 10 seconds before proceeding to next article.
    
    Don't delay if: processing first article, or if current article was skipped due to validation failure.
  </action>
  <verify>
    <automated>timeout 30s node cli.js --input data/part_000000.csv --output test_output/ 2>&1 | grep -E "(Waiting.*seconds|Processing article [2-3])" | head -4</automated>
  </verify>
  <done>CLI waits 10 seconds between API requests with user-visible countdown messages</done>
</task>

<task type="auto">
  <name>task 2: add comprehensive API error handling</name>
  <files>cli.js</files>
  <action>
    Implement robust error handling for all API-related failures without crashing the application.
    
    Error scenarios to handle:
    - Network timeouts (set 30-second request timeout)
    - HTTP error status codes (404, 500, 503, etc.)
    - DNS resolution failures
    - Connection refused errors
    - Malformed API responses
    - JSON parsing errors if applicable
    
    Error handling strategy:
    - Wrap fetch calls in try-catch blocks
    - Log specific error types with descriptive messages
    - Increment skippedCount for failed requests
    - Continue processing remaining articles
    - Show user-friendly error messages: "Warning: Failed to fetch [URL] - [reason]"
    - Maintain overall success/failure counts in final summary
    
    Never crash the entire process due to individual article failures.
  </action>
  <verify>
    <automated>node cli.js --input data/part_000000.csv --output test_output/ 2>&1 | grep -E "(Warning.*Failed|Error handling|catch.*error)" | head -5</automated>
  </verify>
  <done>All API errors are handled gracefully, CLI continues processing after failures</done>
</task>

<task type="auto">
  <name>task 3: enhance progress reporting and final summary</name>
  <files>cli.js</files>
  <action>
    Improve user feedback with detailed progress reporting and comprehensive final summary.
    
    Progress enhancements:
    - Show current status: "Processing article N/M: [URL]"
    - Show API status: "Fetching...", "Success", "Failed - [reason]"
    - Show rate limiting: "Waiting 10 seconds..." with optional countdown
    - Estimate remaining time based on processed articles and 10s delays
    
    Final summary improvements:
    - Total articles processed from CSV
    - Successful API fetches count
    - Skipped due to validation failures
    - Failed due to API/network errors
    - Total processing time
    - Average time per article (including delays)
    
    Format: "Processed 50 articles in 8 minutes: 45 successful, 3 skipped (invalid URLs), 2 failed (network errors)"
  </action>
  <verify>
    <automated>node cli.js --input data/part_000000.csv --output test_output/ 2>&1 | tail -10 | grep -E "(Processed.*articles|successful.*failed.*skipped)"</automated>
  </verify>
  <done>Enhanced progress reporting shows detailed status and comprehensive final summary</done>
</task>

</tasks>

<verification>
- [ ] CLI implements 10-second delays between API requests
- [ ] Rate limiting messages are visible to user
- [ ] Network errors don't crash the application
- [ ] HTTP error codes are handled gracefully
- [ ] Timeout errors are handled with clear messages
- [ ] Final summary includes detailed success/failure breakdown
- [ ] Processing continues after individual article failures
- [ ] User sees estimated remaining time during processing
</verification>

<success_criteria>
- User runs CLI and sees 10-second delays between API requests
- Network failures show "Warning: Failed to fetch [URL] - [specific reason]"
- CLI never crashes from API errors, always completes processing
- Final summary shows breakdown: "X successful, Y skipped, Z failed"
- Total processing time is reported with averages
- Rate limiting is clearly visible: "Waiting 10 seconds before next request..."
- All error types (timeout, 404, DNS, etc.) are handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-extraction/02-02-SUMMARY.md`
</output>