---
phase: 02-content-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [cli.js]
autonomous: true
requirements: [FETCH-03, API-01, API-04]

must_haves:
  truths:
    - "User sees URLs being validated before API calls"
    - "Valid URLs are sent to defuddle.md API service"
    - "API responses return markdown content with frontmatter"
    - "CLI processes API responses and extracts article content"
  artifacts:
    - path: "cli.js"
      provides: "URL validation function"
      min_lines: 15
    - path: "cli.js"  
      provides: "defuddle.md API integration"
      exports: ["fetchArticleContent"]
  key_links:
    - from: "cli.js"
      to: "defuddle.md API"
      via: "HTTP fetch call"
      pattern: "fetch.*defuddle\\.md"
    - from: "API response processing"
      to: "markdown content extraction"
      via: "response parsing"
      pattern: "response\\.text\\(\\)"
---

<objective>
Integrate URL validation and defuddle.md API calls for content extraction

Purpose: Enable the CLI to validate article URLs and fetch their content using the defuddle.md service
Output: URL validation system and working API integration that returns markdown content
</objective>

<execution_context>
@$HOME/.config/Claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-and-csv-foundation/01-01-SUMMARY.md
@.planning/phases/01-cli-and-csv-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key interfaces from existing codebase that this plan will use -->

From cli.js (existing CSV processing):
```javascript
// Article data structure from CSV parsing
const articleData = {
  title: article.title || 'Untitled',
  url: article.url.trim(),
  time_added: article.time_added || '',
  tags: article.tags || ''
};

// Progress reporting pattern
console.log(`Processing article ${articleNum}/${articles.length}...`);

// Error handling pattern  
console.warn(`Warning: Skipping article ${articleNum} - missing URL`);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: implement URL validation function</name>
  <files>cli.js</files>
  <action>
    Create a validateUrl() function that performs basic URL format validation before sending to API.
    
    Requirements:
    - Check for valid protocol (http/https)
    - Check for valid domain format
    - Reject empty, null, or malformed URLs
    - Return boolean true/false for validity
    - Log validation failures with clear messages
    
    Integration: Call validateUrl() before attempting API requests in the existing article processing loop.
    Pattern: If URL invalid, increment skippedCount and continue to next article.
  </action>
  <verify>
    <automated>node cli.js --input data/part_000000.csv --output test_output/ 2>&1 | grep -E "(Processing article|Warning.*URL|validation)"</automated>
  </verify>
  <done>URLs are validated before processing, invalid URLs are skipped with warning messages</done>
</task>

<task type="auto">
  <name>task 2: integrate defuddle.md API calls</name>
  <files>cli.js</files>
  <action>
    Replace the placeholder article processing with actual defuddle.md API integration.
    
    Implementation:
    - Use Node.js built-in fetch() for HTTP requests
    - API endpoint pattern: `https://defuddle.md/${encodedUrl}`
    - Encode URLs properly using encodeURIComponent()
    - Handle API responses that return markdown with YAML frontmatter
    - Extract the markdown content from the response body
    - Update progress messages to show "Fetching: [URL]" during API calls
    
    Error handling: Log network errors but don't crash - continue to next article.
    Success criteria: Valid articles return markdown content that can be extracted.
  </action>
  <verify>
    <automated>node cli.js --input data/part_000000.csv --output test_output/ 2>&1 | grep -E "(Fetching:|successful.*articles)" | head -10</automated>
  </verify>
  <done>CLI successfully fetches article content from defuddle.md API and processes responses</done>
</task>

<task type="auto">
  <name>task 3: process API responses and extract content</name>
  <files>cli.js</files>
  <action>
    Implement response processing to extract markdown content from defuddle.md API responses.
    
    Response format handling:
    - API returns markdown with YAML frontmatter (---\ntitle: "..."\n---\ncontent)
    - Extract the main content after the frontmatter
    - Preserve the original article metadata (title, URL, tags from CSV)
    - Handle responses that might not have frontmatter
    - Store extracted content for markdown file creation (preparation for Phase 3)
    
    Update the article processing to store the fetched content alongside CSV metadata.
    Add success/failure counting for API operations.
  </action>
  <verify>
    <automated>node cli.js --input data/part_000000.csv --output test_output/ 2>&1 | grep -E "(successful.*articles|API.*response)" | tail -5</automated>
  </verify>
  <done>API responses are processed, markdown content is extracted and stored with article metadata</done>
</task>

</tasks>

<verification>
- [ ] CLI validates URLs before making API calls
- [ ] Invalid URLs are skipped with warning messages  
- [ ] Valid URLs are sent to defuddle.md API service
- [ ] API responses return markdown content
- [ ] Markdown content is extracted and processed
- [ ] Progress reporting shows URL validation and API fetching status
- [ ] Error handling continues processing after API failures
</verification>

<success_criteria>
- User runs CLI and sees "Processing article N/M..." followed by URL validation
- Valid URLs trigger "Fetching: [URL]" messages
- Invalid URLs show "Warning: Skipping article N - invalid URL format"
- API calls return markdown content that is processed and stored
- Final summary shows count of successful API fetches vs. skipped articles
- No crashes occur from network errors or API failures
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-extraction/02-01-SUMMARY.md`
</output>