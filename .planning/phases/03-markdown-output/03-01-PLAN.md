---
phase: 03-markdown-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/file-writer.js",
  "src/filename-utils.js",
  "tests/file-writer.test.js",
  "tests/filename-utils.test.js",
  "package.json"
]
autonomous: true
requirements: ["MD-01", "MD-02", "MD-03"]
user_setup: []

must_haves:
  truths:
    - "CLI creates output directory if it doesn't exist"
    - "Article titles become valid cross-platform filenames"
    - "Duplicate titles get unique numbers (title-1.md, title-2.md)"
    - "File writing errors don't crash the CLI"
  artifacts:
    - path: "src/file-writer.js"
      provides: "Directory creation and file writing logic"
      min_lines: 50
    - path: "src/filename-utils.js"
      provides: "Filename sanitization and conflict resolution"
      exports: ["sanitizeFilename", "resolveConflicts"]
    - path: "tests/file-writer.test.js"
      provides: "File operations testing"
      min_lines: 30
  key_links:
    - from: "src/filename-utils.js"
      to: "sanitize-filename"
      via: "npm dependency"
      pattern: "require.*sanitize-filename"
    - from: "src/file-writer.js"
      to: "src/filename-utils.js"
      via: "module import"
      pattern: "require.*filename-utils"
---

<objective>
Create robust file system operations for markdown output with sanitized filenames and conflict resolution.

Purpose: Implements MD-01, MD-02, MD-03 requirements for reliable file creation from article data
Output: File writing modules with comprehensive error handling and cross-platform filename support
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-markdown-output/03-RESEARCH.md
@.planning/phases/01-cli-and-csv-foundation/01-01-SUMMARY.md
@.planning/phases/01-cli-and-csv-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: install sanitize-filename dependency</name>
  <files>package.json</files>
  <behavior>
    - sanitize-filename package added to dependencies
    - Package version is latest stable (^1.6.0 or current)
  </behavior>
  <action>Add sanitize-filename npm package to dependencies for robust cross-platform filename sanitization. Use `npm install sanitize-filename --save` to add to package.json dependencies section.</action>
  <verify>
    <automated>npm list sanitize-filename</automated>
  </verify>
  <done>sanitize-filename appears in package.json dependencies and node_modules exists</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: create filename sanitization utilities</name>
  <files>src/filename-utils.js, tests/filename-utils.test.js</files>
  <behavior>
    - sanitizeFilename() converts titles to safe filenames
    - Handles special characters: <, >, :, ", /, \, |, ?, *
    - Replaces spaces with hyphens
    - Limits length to 200 characters (safe across platforms)
    - Falls back to "untitled-article" for empty/invalid titles
    - resolveConflicts() appends numbers for duplicate filenames
  </behavior>
  <action>Create src/filename-utils.js module with sanitizeFilename() and resolveConflicts() functions. sanitizeFilename() uses sanitize-filename library with options for cross-platform safety. resolveConflicts() checks fs.existsSync() and appends incremental numbers. Create comprehensive tests covering Unicode, special characters, edge cases, and conflict resolution scenarios.</action>
  <verify>
    <automated>npm test -- --testPathPattern=filename-utils</automated>
  </verify>
  <done>Filename utilities handle all edge cases and pass cross-platform tests</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: create file writer with directory handling</name>
  <files>src/file-writer.js, tests/file-writer.test.js</files>
  <behavior>
    - writeArticleFile() creates output directory if missing
    - Accepts {title, content, outputDir} parameters  
    - Uses filename-utils for safe filename generation
    - Handles file system errors gracefully (permissions, disk full)
    - Returns {success: boolean, filepath: string, error: string}
    - Continues processing on individual file failures
  </behavior>
  <action>Create src/file-writer.js with writeArticleFile() function. Use fs.mkdirSync() with recursive: true for directory creation. Integrate filename-utils for sanitization and conflict resolution. Implement comprehensive error handling with try/catch blocks. Return structured result object for calling code. Write tests covering directory creation, file writing, error conditions, and integration with filename utilities.</action>
  <verify>
    <automated>npm test -- --testPathPattern=file-writer</automated>
  </verify>
  <done>File writer creates directories, handles all file system errors, and integrates cleanly with filename utilities</done>
</task>

</tasks>

<verification>
Manual verification:
1. Run CLI with test data - verify output directory is created
2. Test with articles having identical titles - verify numbered suffixes
3. Test with articles containing special characters - verify clean filenames
</verification>

<success_criteria>
- Output directory creation works reliably (MD-01)
- All article titles become valid, readable filenames (MD-02) 
- Duplicate titles resolve to unique files with numbers (MD-03)
- File system errors are handled gracefully without CLI crashes
- All automated tests pass on multiple platforms
</success_criteria>

<output>
After completion, create `.planning/phases/03-markdown-output/03-01-SUMMARY.md`
</output>