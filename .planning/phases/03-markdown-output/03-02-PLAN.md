---
phase: 03-markdown-output
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [
  "src/metadata.js",
  "src/file-writer.js",
  "cli.js",
  "tests/metadata.test.js",
  "tests/integration.test.js"
]
autonomous: true
requirements: ["MD-04"]
user_setup: []

must_haves:
  truths:
    - "Every markdown file contains YAML frontmatter with title, URL, date"
    - "Metadata format is valid YAML and parseable"
    - "Integration works end-to-end from Phase 2 processing"
    - "CLI processes complete articles with proper file output"
  artifacts:
    - path: "src/metadata.js"
      provides: "YAML frontmatter generation"
      exports: ["createMetadata", "formatFrontmatter"]
    - path: "cli.js"
      provides: "Integration point calling file-writer"
      contains: "require.*file-writer"
    - path: "tests/integration.test.js"
      provides: "End-to-end file writing tests"
      min_lines: 40
  key_links:
    - from: "cli.js"
      to: "src/file-writer.js"
      via: "function call"
      pattern: "writeArticleFile\\("
    - from: "src/metadata.js"
      to: "YAML formatting"
      via: "template string"
      pattern: "---\\n.*---"
    - from: "src/file-writer.js"
      to: "src/metadata.js"
      via: "module import"
      pattern: "require.*metadata"
---

<objective>
Complete markdown file output with proper metadata formatting and CLI integration.

Purpose: Implements MD-04 requirement for metadata inclusion and integrates file writing into the main CLI workflow
Output: Complete markdown files with YAML frontmatter and end-to-end CLI functionality
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-markdown-output/03-RESEARCH.md
@.planning/phases/03-markdown-output/03-01-SUMMARY.md
@.planning/phases/01-cli-and-csv-foundation/01-01-SUMMARY.md
@.planning/phases/01-cli-and-csv-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: create metadata formatting module</name>
  <files>src/metadata.js, tests/metadata.test.js</files>
  <behavior>
    - createMetadata() accepts {title, url, timestamp} parameters
    - formatFrontmatter() generates valid YAML frontmatter block
    - Handles special characters in titles and URLs (YAML escaping)
    - Formats timestamps as ISO 8601 strings
    - Returns complete markdown with frontmatter + content
  </behavior>
  <action>Create src/metadata.js with functions to generate YAML frontmatter. createMetadata() builds metadata object from article data. formatFrontmatter() creates properly formatted YAML block with --- delimiters. Handle YAML escaping for special characters in titles/URLs. Create tests verifying YAML validity, special character handling, and proper formatting.</action>
  <verify>
    <automated>npm test -- --testPathPattern=metadata</automated>
  </verify>
  <done>Metadata module generates valid YAML frontmatter for all article data types</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: integrate metadata into file writer</name>
  <files>src/file-writer.js</files>
  <behavior>
    - writeArticleFile() now accepts {title, content, url, timestamp, outputDir}
    - Combines metadata frontmatter with article content
    - Creates complete markdown file with YAML header
    - Maintains existing filename and error handling behavior
  </behavior>
  <action>Update src/file-writer.js to import metadata module and integrate formatFrontmatter() into writeArticleFile(). Modify function signature to accept url and timestamp parameters. Combine YAML frontmatter with article content before writing file. Update tests to verify complete markdown format including metadata.</action>
  <verify>
    <automated>npm test -- --testPathPattern=file-writer</automated>
  </verify>
  <done>File writer creates complete markdown files with proper YAML frontmatter</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: integrate file writing into CLI workflow</name>
  <files>cli.js, tests/integration.test.js</files>
  <behavior>
    - CLI imports and calls file-writer module after API processing
    - Passes article data from Phase 2 to writeArticleFile()
    - Handles file writing errors without stopping processing
    - Reports file writing success/failure in console output
    - Maintains existing CLI argument parsing and CSV processing
  </behavior>
  <action>Update cli.js to require src/file-writer module. Add file writing step after successful API content extraction (Phase 2 integration point). Pass {title, content, url, timestamp, outputDir} to writeArticleFile(). Handle returned errors gracefully with console logging. Update progress reporting to include file writing status. Create integration tests that verify end-to-end processing creates proper markdown files.</action>
  <verify>
    <automated>npm test -- --testPathPattern=integration</automated>
  </verify>
  <done>CLI successfully processes articles and creates complete markdown files with metadata</done>
</task>

</tasks>

<verification>
End-to-end verification:
1. Run CLI with sample CSV containing 2-3 articles
2. Verify output directory is created with markdown files
3. Open generated files - confirm YAML frontmatter is valid and readable
4. Check filenames are sanitized and conflicts resolved properly
</verification>

<success_criteria>
- All markdown files include proper YAML frontmatter with title, URL, and date (MD-04)
- YAML formatting is valid and parseable by standard libraries
- CLI integration works seamlessly with Phase 2 content extraction
- End-to-end processing creates complete, well-formed markdown archives
- All automated tests demonstrate proper metadata handling
</success_criteria>

<output>
After completion, create `.planning/phases/03-markdown-output/03-02-SUMMARY.md`
</output>